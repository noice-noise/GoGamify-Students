console.log("Preload script executed");

window.onload = function () {
  document.querySelector("body").classList.toggle("hidden");

  // const loader = document.createElement("div");
  // loader.id = "loaderx";
  // loader.classList = "flex h-screen items-center justify-center";
  // loader.innerHTML = `<div class="h-16 w-16 animate-spin rounded-full border-[0.5rem] border-foreground border-t-primary"></div>`;

  // document.querySelector("nav").appendChild(loader);
};
